// Spec §4.4: star point vertex shader. Sizes points on a log-magnitude scale
// (clamped at the bright end), passes B−V for the fragment-side colour lookup,
// and discards points beyond the magnitude-limit uniform without geometry reupload.
