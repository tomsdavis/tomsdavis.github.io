// Spec §5.1: bottom drawer on phone-portrait (collapsed → row of essential icons;
// expanded → scrollable sliders + toggles). Same content as a fixed side panel
// on tablet/desktop. All controls ≥ 44×44 CSS pixels.
