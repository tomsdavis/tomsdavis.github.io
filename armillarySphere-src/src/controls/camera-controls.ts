// Spec §4.2: custom orbit controller via the Pointer Events API.
// Rotate (drag), zoom (pinch/wheel) clamped to [~1.05, ~5] in scene units; pan
// disabled in v1. Inertia and damping with conservative parameters.
