// Spec §4.4: single THREE.Points object backed by the BSC5 binary buffer.
// Custom point shader: log-magnitude sizing, B−V → blackbody colour, fragment
// discard below the magnitude-limit uniform (no geometry reupload on slider).
