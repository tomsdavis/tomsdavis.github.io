/** Default number of columns in the grid */
export const DEFAULT_COLUMNS = 8;

/** Default number of rows in the grid */
export const DEFAULT_ROWS = 4;

/** Maximum number of columns allowed */
export const MAX_COLUMNS = 16;

/** Maximum number of rows allowed */
export const MAX_ROWS = 64;

/** Minimum octave offset */
export const MIN_OCTAVE = 0;

/** Maximum octave offset */
export const MAX_OCTAVE = 8;

/** Default octave for new notes */
export const DEFAULT_OCTAVE = 4;

/** MIDI note number for C0 */
export const C0_MIDI = 12;

/** Reference frequency for A4 (440 Hz standard tuning) */
export const A4_HZ = 440;

/** MIDI note number for A4 */
export const A4_MIDI = 69;

/** Cell size in pixels */
export const CELL_SIZE = 64;

/** Drop zone thresholds (fraction of cell width) */
export const DROP_ZONE_EDGE = 0.2;

/** Minimum palette octave offset in relative mode */
export const MIN_PALETTE_OCTAVE_RELATIVE = -4;

/** Maximum palette octave offset in relative mode */
export const MAX_PALETTE_OCTAVE_RELATIVE = 4;

/** Minimum pointer movement in pixels before a pointerdown is treated as a drag */
export const DRAG_THRESHOLD = 6;
